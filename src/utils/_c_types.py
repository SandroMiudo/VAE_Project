from typing import Tuple, Mapping, Any
from numpy import ndarray

type Properties = Tuple[int, int]
type Globals = Tuple[int, int, float, float]
type Strategy = str
type _size = int

type NdArrayLike = ndarray
type MapLike = Mapping[str, Any]